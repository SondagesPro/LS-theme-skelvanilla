{# create a shortcut for aSurveyInfo.aNavigator #}
{% set aNavigator = aSurveyInfo.aNavigator %}

<!-- move next, previous and submit -->
<!-- If you want multiple navigator , set a navigatorid to another id (only alphanumeric)  -->

{% set navigatorid = "" %}
{{ include('./subviews/navigation/navigator_move.twig') }}
<!-- Extra navigator part -->
{{ include('./subviews/navigation/navigator_complement.twig') }}
