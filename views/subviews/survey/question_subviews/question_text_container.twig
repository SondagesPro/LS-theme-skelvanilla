<!-- Question text -->
{% if ( aSurveyInfo.options.questionhtag == 'on' ) %}
    {% set tagHeader = 'header' %}
{% else %}
    {% set tagHeader = 'div' %}
{% endif %}
<{{ tagHeader }} class="{{ aSurveyInfo.class.questiontitlecontainer }} col-xs-12 " {{ aSurveyInfo.attr.questiontitlecontainer }} >
    {{ include('./subviews/survey/question_subviews/question_text_content.twig') }}
</{{ tagHeader }}>
